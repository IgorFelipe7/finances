VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# MVP tradeoff: called directly from the browser (dangerouslyAllowBrowser).
# This exposes the key to anyone inspecting network traffic — move behind a
# server/edge function before shipping past prototype stage.
VITE_OPENAI_API_KEY=
